name: Deploy to GitHub Pages
on:
push:
branches:
 * main
jobs:
deploy_site:
runs-on: ubuntu-latest
steps:
 * name: Checkout Code
   uses: actions/checkout@v4
 * name: Setup Node.js
   uses: actions/setup-node@v4
   with:
   node-version: 20
   cache: npm
